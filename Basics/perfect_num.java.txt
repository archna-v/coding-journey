class Solution {
    static boolean isPerfect(int n)
    {
        int res=0;
        for(int i=1;i<=n;i++)
        {
            if(n%i==0&&i!=n)
            {
               res=res+i;
            }
        }
        if(res==n) return true;
        else return false;
        
    }
};